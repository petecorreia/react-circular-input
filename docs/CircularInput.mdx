---
name: CircularInput
route: /circular-input
---

import { useState } from 'react'
import { Playground } from 'docz'
import { CircularInput } from '../src'

# CircularInput

Provides all the logic and context for the children components.

```jsx
<CircularInput value={value} radius={100} onChange={setValue} />
```

## Props

```jsx
value: number // range: [0-1]
radius?: number
onChange?: (value: number) => any
// it's an <svg> under the hood:
...HTMLProps<SVGSVGElement>
// but the following props are disallowed to support functionality:
🚫 ref
🚫 width
🚫 height
🚫 viewBox
🚫 onClick
```

- 👉 If you don't provide children it defaults to using progress, track and thumb components
- 👉 If you provide `onChange` it sets the value on click and inner components set value on click and drag.

## Examples

<Playground>
	{() => {
		const [value, setValue] = useState(0.25)
		return <CircularInput value={value} radius={100} onChange={setValue} />
	}}
</Playground>

<Playground>{<CircularInput value={0.5} />}</Playground>

<Playground>{<CircularInput value={0.5} radius={150} />}</Playground>
