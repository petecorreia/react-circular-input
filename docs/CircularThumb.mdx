---
name: CircularThumb
route: /circular-thumb
---

import { useState } from 'react'
import { Playground } from 'docz'
import { CircularInput, CircularThumb } from '../src'

# CircularThumb

Displays a handle at the value point.

```jsx
<CircularInput value={0.25}>
	<CircularThumb />
</CircularInput>
```

## Props

```jsx
// it's an svg circle under the hood:
...SVGProps<SVGCircleElement>
// but the following props are disallowed to support functionality:
🚫 ref
🚫 cx
🚫 cy
```

- 👉 If you provide `onChange` to `CircularInput`, it sets the value on click or drag.

## Examples

<Playground>
	{() => {
		const [value, setValue] = useState(0.25)
		return (
			<CircularInput value={value} onChange={setValue}>
				<CircularThumb />
			</CircularInput>
		)
	}}
</Playground>

<Playground>
	{
		<CircularInput value={0.75}>
			<CircularThumb fill="red" />
		</CircularInput>
	}
</Playground>
