---
name: CircularTrack
route: /circular-track
---

import { useState } from 'react'
import { Playground } from 'docz'
import { CircularInput, CircularTrack } from '../src'

# CircularTrack

Displays the value circumference path.

This component by itself is not very exciting... 😅

```jsx
<CircularInput value={0.25}>
	<CircularTrack />
</CircularInput>
```

## Props

```jsx
// it's an svg circle under the hood:
...SVGProps<SVGCircleElement>
// but the following props are disallowed to support functionality:
🚫 ref
🚫 cx
🚫 cy
🚫 r
```

- 👉 If you provide `onChange` to `CircularInput`, it sets the value on click or drag.

## Examples

<Playground>
	{
		<CircularInput value={0.25}>
			<CircularTrack />
		</CircularInput>
	}
</Playground>

<Playground>
	{
		<CircularInput value={0.25}>
			<CircularTrack stroke="red" />
		</CircularInput>
	}
</Playground>
